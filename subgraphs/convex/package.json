{
  "name": "convex",
  "license": "MIT",
  "version": "1.0.0",
  "files": [
    "generated"
  ],
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ convex-community/convex",
    "deploy-hosted": "graph deploy --product hosted-service convex-community/convex",
    "deploy-studio": "graph deploy --studio convex",
    "create-local": "graph create --node http://localhost:8020/ convex/convex",
    "remove-local": "graph remove --node http://localhost:8020/ convex/convex",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 convex/convex"
  }
}
