{
  "name": "gamma",
  "license": "MIT",
  "version": "1.0.0",
  "files": [
    "generated"
  ],
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ convex-community/negative-gamma",
    "deploy-hosted": "graph deploy --product hosted-service convex-community/negative-gamma",
    "deploy-studio": "graph deploy --studio negative-gamma",
    "create-local": "graph create --node http://localhost:8020/ convex/negative-gamma",
    "remove-local": "graph remove --node http://localhost:8020/ convex/negative-gamma",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 convex/negative-gamma"
  }
}
