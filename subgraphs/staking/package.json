{
  "name": "staking",
  "license": "MIT",
  "version": "1.0.0",
  "files": [
    "generated"
  ],
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ convex-community/convex-staking",
    "deploy-hosted": "graph deploy --product hosted-service convex-community/convex-staking",
    "deploy-studio": "graph deploy --studio convex-staking",
    "create-local": "graph create --node http://localhost:8020/ convex/convex-staking",
    "remove-local": "graph remove --node http://localhost:8020/ convex/convex-staking",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 convex/convex-staking"
  }
}
