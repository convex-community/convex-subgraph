{
  "name": "votium",
  "license": "MIT",
  "version": "1.0.0",
  "files": [
    "generated"
  ],
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ convex-community/votium-bribes",
    "deploy-hosted": "graph deploy --product hosted-service convex-community/votium-bribes",
    "deploy-studio": "graph deploy --studio votium-bribes",
    "create-local": "graph create --node http://localhost:8020/ convex/votium",
    "remove-local": "graph remove --node http://localhost:8020/ convex/votium",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 convex/votium"
  }
}
